# ==============================================================================
# PhilGEPS Frontend Environment Configuration
# ==============================================================================
# For use with: docker-compose.frontend.yml
# Copy to .env: cp env.frontend.example .env
# ==============================================================================

# ==============================================================================
# Frontend Configuration
# ==============================================================================

# Port for frontend server
FRONTEND_PORT=3000

# ==============================================================================
# Backend API Configuration
# ==============================================================================

# Backend API URL - MUST be accessible from user's browser!
# This is the URL where the frontend will make API requests
# 
# Development: http://localhost:3200
# Production:  https://philgeps-api.simple-systems.dev
VITE_API_URL=http://localhost:3200

# ==============================================================================
# Example Configurations
# ==============================================================================

# Development (both running locally):
# FRONTEND_PORT=3000
# VITE_API_URL=http://localhost:3200

# Production (separate servers):
# FRONTEND_PORT=80
# VITE_API_URL=https://philgeps-api.simple-systems.dev

# ==============================================================================
# Important Notes
# ==============================================================================
#
# 1. VITE_API_URL is baked into the build at BUILD TIME
#    You must rebuild if you change it:
#    docker compose -f docker-compose.frontend.yml build --no-cache
#
# 2. Backend must have CORS configured to allow frontend domain:
#    Backend .env should have:
#    CORS_ALLOWED_ORIGINS=https://philgeps.simple-systems.dev
#
# 3. For production, use HTTPS URLs for security
#
# ==============================================================================

